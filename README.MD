# Convert Metr.at json format files in csv files that racerender likes to read
## Usage
just download and run **node convert.js inputfile.json** program will parse json and ouput into inputfile.json.out.csv 